{
  "mcpServers": {
    "file-classifier": {
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "-e", "OLLAMA_HOST=http://host.docker.internal:11434",
        "-e", "OLLAMA_MODEL_NAME=gpt-oss:20b-cloud",
        "-v", "${USER_HOME}:/home/user",
        "docker-mcp"
      ]
    }
  }
}
